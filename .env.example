# Required — DALL-E 3 image generation
OPENAI_API_KEY=

# S3 storage (optional — defaults to local filesystem)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_BUCKET=
# S3_REGION=us-east-1

# Storage mode: "s3" or "local" (default: local)
# STORAGE_MODE=local
